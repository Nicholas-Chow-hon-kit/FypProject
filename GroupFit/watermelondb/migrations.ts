import { schemaMigrations } from '@nozbe/watermelondb/Schema/migrations'

export default schemaMigrations({
  migrations: [
    // We'll add migration definitions here later, this is only needed when we update the database
    // schema and we would like to have a migration to update the database
  ],
})